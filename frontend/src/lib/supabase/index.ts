export { createClient, getSupabaseClient } from './client'
export { createServerSupabaseClient } from './server'
export { updateSession, protectedRoutes, adminRoutes, authRoutes } from './middleware'
